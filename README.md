# PyVault
A secure, zero-knowledge unified vault. The system must ensure that the server infrastructure (OCI) only stores encrypted data and has no capability to read it, keeping data safe even if the server or database is compromised.
